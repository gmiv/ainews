"""ai_news_feed — a colorful curses TUI for AI news.

Fetches ~30 RSS feeds in parallel, enriches them with GPT-5.4 (theme summary,
one-word mood, per-headline sentiment, topic clustering, a ranked top-stories
leaderboard, live web-search grounding, and chat-with-your-feed), and renders
everything in a navigable two-pane terminal UI.
"""
__version__ = "2.0.0"
